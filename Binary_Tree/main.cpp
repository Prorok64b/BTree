#include <iostream>
#include "Node.h"
#include "test.h"

int main()
{
	/*Node* root = new Node();

	root->push(2);
	root->push(4);
	root->push(3);
	root->push(6);
	root->push(5);
	root->push(7);

	root->push(-3);
	root->push(-4);
	root->push(-2);

	root->push(1);*/

	/*std::cout << root->left->id << std::endl;
	std::cout << root->right->left->id << std::endl;
	std::cout << root->right->id << std::endl;*/

	//std::cout << root->remove(-300) << std::endl;

	test();

	system("pause");

	//delete root;
	return 0;
}